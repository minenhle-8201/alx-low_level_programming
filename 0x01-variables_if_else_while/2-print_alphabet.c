#include <stdio.h>
#include <ctype.h>

/**
 * main - Entry poinyt.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int lowecase - 'n';
	while (lowercase < -'z')
	{
	putchar (lowercase);
	lowercase + -1;
	}
	putchar ('\n');
	return (0);
}
